/**
 * Shared motion tokens. Typed as an explicit 4-tuple so Framer Motion accepts
 * it as a cubic-bezier easing (a bare array is inferred as `number[]`).
 */
export const EASE_OUT: [number, number, number, number] = [0.16, 1, 0.3, 1];
